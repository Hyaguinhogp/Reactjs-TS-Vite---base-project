import styled from "styled-components";
import { DefaultContainer, DefaultContent } from "../../styles/global";

export const ModelContainer = styled(DefaultContainer)`
    background-color: var(--principal-bc-01);
`;

export const ModelContent = styled(DefaultContent)`
    background-color: var(--principal-bc-02);
`;